NEWS REGARDING BYTEIO 0.02

- Added flip function family; these functions reverses byte order in given
  numbers. Note that because int8 consists of one byte, so cannot be flipped,
  no functions exist that flip int8 or uint8.

  Prototype:
  <TYPE> byteio_flip_<TYPE> (<TYPE>);
